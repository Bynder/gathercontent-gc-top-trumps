import React from 'react';
import { Button } from '../Button';
import { Info } from '../Icons';

export default {
    title: 'Buttons',
};

export const Buttons = (args) => <>
   <Button>Hello</Button>
   <Button><Info /></Button>
</>;
